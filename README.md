# pd23-schedule
# pd23-schedule — Telegram-бот + WebApp розкладу для ПД-21…ПД-24

Навчальний проєкт з курсу **«Професійна практика програмної інженерії»**.

Проєкт складається з двох частин:

1. **Telegram-бот розкладу** — відповідає в чаті командами `/today`, `/tomorrow`, `/date` і дає текстовий розклад.
2. **WebApp (index.html)** — вбудована веб-сторінка, яка відкривається з бота кнопкою «🌐 Веб-розклад» та показує розклад у красивому інтерфейсі.

Ніяких окремих API чи баз даних немає — **розклад зберігається як звичайний словник у Python та як JS-об’єкт у index.html**.

Автори: **Однорог Іван, Мазур Віталій (ПД-23)**.

---

## Що вміє система

### 1. Telegram-бот (`bot.py`)

- Команда `/start`:
  - вітає користувача;
  - пояснює можливості бота;
  - показує клавіатуру з кнопками:
    - «📆 Сьогодні»
    - «➡️ Завтра»
    - «🌐 Веб-розклад» (відкриває WebApp).
- `/today` — показує розклад на сьогодні для групи (за замовчуванням ПД-23).
- `/tomorrow` — розклад на завтра.
- `/date YYYY-MM-DD` — розклад на конкретну дату (формату `2025-12-03`).
- Обробка натискання кнопок «Сьогодні» / «Завтра» так само, як відповідні команди.
- Формат повідомлення:
  - «Розклад для ПД-23 на 03.12.2025»
  - далі список пар: час, назва, викладач, аудиторія / «Дистанц.».

### 2. Джерело розкладу (`schedule_data.py`)

- Містить словник `SCHEDULE`, де ключ — дата у форматі `YYYY-MM-DD`, значення — список пар.
- Кожна пара — це словник з полями:
  - `time` — час пари;
  - `subject` — назва дисципліни (з типом: лекція, лаба, пз);
  - `teacher` — викладач;
  - `room` — аудиторія або «Дистанц.».
- Функція `get_schedule_for_date(day)` повертає список пар для вказаної дати.
- Функція `format_day_schedule_text(lessons, day)` формує красивий текст для Telegram.

### 3. Налаштування (`config.py`)

- `BOT_TOKEN` — токен з BotFather.
- `GROUP_TITLE` — назва групи за замовчуванням (наприклад, `"ПД-23"`).
- `WEBAPP_URL` — посилання на розгорнутий `index.html` (GitHub Pages або інший хостинг).

### 4. WebApp (`index.html`)

- Односторінковий інтерфейс у темних тонах.
- Елементи керування:
  - кнопка **«Сьогодні»** — показує розклад на поточну дату;
  - кнопка **«Завтра»** — показує розклад на наступний день;
  - поле вибору дати (`input type="date"`);
  - (можна додати) перемикач груп: **ПД-21 / ПД-22 / ПД-23 / ПД-24**.
- Всередині JS є об’єкт `SCHEDULE`, де збережені всі пари по датах та групах.
- Відображення:
  - назва групи;
  - день тижня + дата;
  - список пар з часом, предметом, викладачем і аудиторією.
- Працює як:
  - звичайна веб-сторінка в браузері;
  - WebApp всередині Telegram (через кнопку з бота).

---

## Структура репозиторію

Фактична мінімальна структура проєкту:

```text
pd23-schedule/
├─ bot.py           # логіка Telegram-бота
├─ config.py        # токен, назва групи, URL WebApp
├─ schedule_data.py # дані розкладу та функції форматування
├─ index.html       # WebApp з розкладом
└─ README.md        # цей файл
